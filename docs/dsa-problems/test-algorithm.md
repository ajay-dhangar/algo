---
title: Test Algorithm
tags: [algorithm, test]
complexity:
  time_best: "O(1)"
  time_average: "O(n)"
  time_worst: "O(n^2)"
  space: "O(n)"
---

# Test Algorithm

This is a test algorithm page. The ComplexityCard should appear automatically below the main heading.

```javascript
function testAlgorithm(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = 0; j < arr.length; j++) {
      console.log(arr[i], arr[j]);
    }
  }
}
```
